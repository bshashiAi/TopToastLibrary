jdk:
  - openjdk17

before_install:
  - sdk install java 17.0.12-oracle
  - sdk use java 17.0.12-oracle

install:
  - ./gradlew :toplibrary:assembleRelease :toplibrary:publishToMavenLocal

android:
  compileSdkVersion: 34
  buildToolsVersion: "34.0.0"
